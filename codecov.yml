coverage:
  precision: 2
  round: down
  range: "70...100"
  notify:
    require_ci_to_pass: yes

  status:
    project: yes

    patch: yes

    changes: no

comment:
  layout: "header, diff, changes"
  behavior: default
  require_changes:no

